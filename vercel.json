{
  "installCommand": "npm install && npm --prefix puente install",
  "buildCommand": "npm --prefix puente run typecheck",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
