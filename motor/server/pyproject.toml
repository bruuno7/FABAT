[project]
name = "motor-server"
version = "0.1.0"
description = "Servidor local de la pantalla de mando (privado, no se publica)"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "httpx>=0.27",
    "segno>=1.6",
    "mcp>=1.12,<2",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
