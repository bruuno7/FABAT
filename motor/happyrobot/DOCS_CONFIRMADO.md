# DOCS CONFIRMADO — HappyRobot (equipo)

Confirmado por uso/CEO/docs privadas:

1. Incoming Hook: POST JSON → inicia run; body = output del trigger.
2. Web Call preferido para inbound demo (no PSTN desde ES).
3. No hay integración nativa Telegram.
4. API key nace Viewer; hace falta **Editor** para crear workflows.
5. Cluster EU para hack Spain team.

Pendiente verificar midiendo: latencia voz &lt;300 ms (no afirmar sin medición).
