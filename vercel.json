{
  "installCommand": "npm install && npm --prefix motor/server install",
  "buildCommand": "npm --prefix motor/server run typecheck",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
