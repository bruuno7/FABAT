# Buzón — Bruno (motor/happyrobot)

Fecha: 2026-09-19  
De: instancia puente Telegram (cloud agent)

## Qué se añadió en `motor/happyrobot`

- `webhook_contract.json` — `public_report` + `hr_to_mando` + colores dos capas
- `WORKFLOWS.md` — `fa-webcall`, `fa-entrada-tg`
- `recipes/fa-entrada-tg.md` — receta UI Incoming Hook → extract → webhook MANDO
- `DOCS_CONFIRMADO.md` — notas CEO / EU

## Qué necesito de ti

Cuando el usuario entregue API key **Editor** (opción A) o cree el workflow a mano (B):

1. Crear/publicar `fa-entrada-tg` en development EU
2. Pegar URL hook development a Ana como `HR_HOOK_TG` (fuera de git)
3. Webhook out a `MANDO_CALLBACK_URL/hr/events` con header `x-hr-secret`

Northstars / adversarial del workflow: pendientes de afinar contigo en UI HR.
