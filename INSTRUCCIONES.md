# INSTRUCCIONES FABAT (mínimas)

1. HappyRobot **habla/confirma**; MANDO **prioriza/asigna/replanifica**. Cero LLM en el núcleo del planificador.
2. Demo principal: **Web Call** en navegador. Telegram = canal gratis paralelo (API bridge).
3. Colores dos capas: `perfil_color` (estable) + `triage_color` (incidente).
4. Secretos solo en env / túnel; nunca en git.
5. Leer `TRASPASO.md` antes de tocar motor.
