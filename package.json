{
  "name": "fabat",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm --prefix motor/server test",
    "dev:bridge": "npm --prefix motor/server run dev",
    "poll": "npm --prefix motor/server run poll",
    "build": "npm --prefix motor/server run build",
    "vercel-build": "npm --prefix motor/server run build && mkdir -p public"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=20"
  }
}
