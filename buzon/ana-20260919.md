# Buzón — Ana (motor/server)

Fecha: 2026-09-19  
De: instancia puente Telegram (cloud agent)

## Qué se añadió en `motor/server`

Scaffold TypeScript Express:

- `POST /telegram/webhook` → mapea Update TG → `public_report` → `HR_HOOK_TG`
- `POST /hr/events` → valida contrato → `sendMessage` si hay `reply_text` + `chat_id`
- `npm run poll` para local sin HTTPS
- Tests unitarios del mapeo/contrato (sin secretos)
- `.env.example` documentado

## Qué necesito de ti / del usuario

1. Token BotFather (solo en `.env`, no en git)
2. Decisión HTTPS vs poll
3. Coordinar con Bruno URL Incoming Hook → `HR_HOOK_TG`

No ejecuté setWebhook ni creé bots reales (bloqueado sin token).
