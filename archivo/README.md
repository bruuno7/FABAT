# archivo/ — no es la demo

Aquí vive lo que **ya no entra en el producto** (diseño: un equipo de agentes en HappyRobot + Sala de control). **No se ha borrado nada.** Cada cosa conserva su ruta relativa (`web/` → `archivo/web/`, etc.).

Motivo: demasiadas pantallas, una app Next.js con datos ficticios, herramientas de desarrollo y documentos viejos. El equipo no podía explicar el repo.

El runtime **no carga** este árbol. Las URLs antiguas (`/centro`, `/clasico`, `/duelo`, `/curva`, `/caos`, `/memoria`, `/informe`, `/historial`, `/personal`, `/simulacro`) redirigen a `/` (Sala).

Cómo deshacerlo: ver `docs/LIMPIEZA.md`.
